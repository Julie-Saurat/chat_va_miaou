<div class="container-authentification">
  <h2>Modifier mes infos <% resource_name.to_s.humanize %></h2>

  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :email, required: true, autofocus: true %>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
      <% end %>
      <div class="row">
        <div class="col">
          <%= f.input :password,
                      label: "Mot de passe",
                      hint: "N'écris rien si tu ne souhaites pas le changer",
                      required: false,
                      input_html: { autocomplete: "new-password" } %>
        </div>
        <div class="col">
          <%= f.input :password_confirmation,
                      label: "Confirme ton mot de passe",
                      required: false,
                      input_html: { autocomplete: "new-password" } %>
        </div>
      </div>
      <%= f.input :current_password,
                  label: "Mot de passe actuel",
                  hint: "Nous avons besoin de ton mot de passe actuel pour confirmer les changements :)",
                  required: true,
                  input_html: { autocomplete: "current-password" } %>
    </div>

    <div class="form-actions btn btn-warning">
      <%= f.button :submit, "Mettre à jour" %>
    </div>
  <% end %>

  <div class="cancel-account">
    <h3>Supprimer mon compte</h3>

    <p> Hello ! <br/> Nous sommes désolés d'apprendre que tu souhaites supprimer ton compte ! <%= link_to "Supprimer mon compte", registration_path(resource_name), data: { confirm: "Donne aux boules de poils une seconde chance et clique sur annule pour rester avec nous ! ;) " }, method: :delete %></p>
  </div>



  <%= link_to "Retour", :back %>

</div>



